## 操作系统作业 11
石曜铭 2023K8009929037

**11.1** 现有一个由5块磁盘组成的磁盘阵列，采用RAID-5模式，如图所示。
该磁盘阵列每块盘的磁盘块（block）大小为4KB，每条（strip）含一个块；磁盘的平均寻道时间是5ms，平均旋转速度是12000 RPM（每分钟12000转），传输带宽是200MB/s，请计算：

1）平均来说，从该RAID5阵列上读出一个条带（stripe）的时间是多少？
一个条带包含 4 个数据块，分布在不同的磁盘上，可以并行读取。读取一个 4 KB 块的时间包括寻道时间、旋转延迟和传输时间。

- 平均寻道时间：5 ms

- 平均旋转延迟：半圈时间，转速为 12000 RPM，每转时间 = 60 / 12000 = 5 ms，半圈为 2.5 ms

- 传输时间：$\frac{4 KB}{200 MB/s} = \frac{4096 B}{200 \times 10^6 B/s} = 2.048 × 10^{-5} s = 0.02048$ ms

单块读取时间为 $5 + 2.5 + 0.02048 = 7.52048$ ms。

并行读取 4 个数据块，时间约为单块读取时间，即 7.52 ms。

2）当向该RAID5阵列中写入连续的两个4KB数据块时，平均来说，所需的时间是多少？请考虑这两个数据块属于同一个条带和不同条带的两种情况。

RAID-5 采用读-改-写方式更新数据块和奇偶校验块。

**属于同一个条带**：需更新两个数据块和一个共享奇偶校验块。操作包括并行读取两个旧数据块和旧奇偶校验（约 7.52 ms），然后并行写入两个新数据块和新奇偶校验（约 7.52 ms）。总时间 ≈ 7.52 + 7.52 = 15.04 ms。

**属于不同条带**：每个数据块独立进行读-改-写，每个操作约 15.04 ms。由于连续跨条带的两个块通常访问相同的磁盘（如块 3 和块 4 都涉及磁盘 3 和磁盘 4），操作串行执行，总时间 ≈ 2 × 15.04 = 30.08 ms。

**11.2** 现有一块320GB的SLC SSD，它的擦写上限(P/E cycles)是 200,000 次。假设SSD FTL能将写均匀分布在所有的闪存页上，若以每秒发 300,000 个写请求的速率写，请问多长时间这块SSD 会被磨穿?

SSD 容量 320 GB，P/E cycles 为 200,000，FTL 均匀磨损。写请求速率 300,000 个/秒，假设每个请求写入 4 KB（典型块大小）。

- 总可写入数据量 = 容量 $\times$ P/E cycles = $320 \times 10^9 B × 200000 = 6.4 \times 10^{16} B$。
- 写数据速率 = 300000 请求/秒 $\times$ 4096 B/请求 = $1.2288 \times 10^9$ B/s。
- 磨穿时间 = 总可写入数据量 / 写数据速率 = $\frac{6.4 \times 10^{16}}{1.2288 \times 10^9} ≈ 5.208 × 10⁷$ s ≈ $1.65$ y。

**11.3** 现有一块SSD，每个擦除块有256页，且它读一页的延迟是20微秒，写一页的延迟是 60微秒，擦除一块的时间是1毫秒。如果该SSD的FTL采用混合映射，分下面3种情况，计算回收一个块需要的时间。
1) **Switch merge**
回收的块中无有效页，仅需擦除。时间 = 1 ms。

2) **Partial merge 且假设块中有效页为 50%**
有效页数 = 256 × 50% = 128 页。
- 读有效页时间 = 128 × 20 μs = 2560 μs = 2.56 ms。
- 写有效页时间 = 128 × 60 μs = 7680 μs = 7.68 ms。
- 擦除时间 = 1 ms。
总时间 = 2.56 + 7.68 + 1 = 11.24 ms。

3) **Full merge**
有效页数 = 256 页。
- 读全部页时间 = 256 × 20 μs = 5120 μs = 5.12 ms。
- 写全部页时间 = 256 × 60 μs = 15360 μs = 15.36 ms。
- 擦除时间 = 1 ms。
总时间 = 5.12 + 15.36 + 1 = 21.48 ms。